# Instance ID: i-02c7ee74fa86fffa4

# Aquí definiremos los recursos después de importarlos
resource "aws_instance" "existing_ec2" {
  # Los atributos se llenarán después de la importación
}

resource "aws_iam_role" "existing_role" {
  # Los atributos se llenarán después de la importación
}

resource "aws_iam_instance_profile" "existing_profile" {
  # Los atributos se llenarán después de la importación
}